# Local example only. Never commit a real .env file.
# On Render, add these under Environment > Environment Variables.
AUTH_EMAIL=company@example.com
AUTH_PASSWORD=replace-with-a-long-unique-password
AUTH_COMPANY=Company Name
NODE_ENV=production
